{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Grep|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "go -C ${CLAUDE_PLUGIN_ROOT} run . hook"
          }
        ]
      }
    ]
  }
}
